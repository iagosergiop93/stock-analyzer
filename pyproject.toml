[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stockanalyzer"
version = "0.1.0"
requires-python = ">=3.6"
dependencies = [
    "sqlalchemy",
]

[tool.setuptools]
packages = ["stockanalyzer"] 